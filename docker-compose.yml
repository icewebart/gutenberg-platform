services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: gutenberg
      POSTGRES_PASSWORD: gutenberg
      POSTGRES_DB: gutenberg_dev
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
